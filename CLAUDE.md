# CLAUDE.md — 開発プロセス要件(必読・遵守)

このリポジトリで作業する際の必須ルール。**毎セッション、これに従うこと。**

## 1. 場当たり修正の禁止

バグ修正・挙動変更・API変更の前に、必ずこの順序を踏む:

1. **根本原因の実証**(推測で直さない。再現・ソース確認・一次資料での裏取り)
2. **選択肢2つ以上のトレードオフ評価**
3. **`docs/design-notes/YYYY-MM-DD-<topic>.md` に記録**(採用案と棄却理由)
4. **回帰ガードテスト付きで実装**

軽微に見える修正(テストの直し・ドキュメント整合)でも、既存の設計ノートに
追記するか、コミットメッセージで根拠を示す。「動いたから良し」で先に進まない。

## 2. コミットは検証グリーン時のみ

- コード変更を含むコミットは、Linux検証サンドボックス(コンパイル+EditModeテスト
  全グリーン)を通してから行う。サンドボックスはコンテナ揮発のためセッションごとに
  再構築が必要(構築記録: `docs/verify/2026-07-30-linux-batch-results.md`。
  ライセンスは検証専用アカウントで Licensing Client の
  `--activate-all --include-personal`。認証情報はリポジトリに書かない)
- 検証環境が使えない場合は静的ゲート(ASCII監査・meta整合・JSON検証)まで行い、
  未検証である旨をコミットメッセージと報告に明記する

## 3. コスト方針(サブエージェント/ワークフローのモデル選定)

ユーザーの上位モデル利用枠は限られている。**既定継承(最上位モデル)を漫然と
使わない**こと:

- 機械的作業(一括置換・抽出・定型変換・ファイル列挙)= **haiku**
- 検証・標準実装・通常レビュー・監査 = **sonnet**
- コア設計・難所の分析(サロゲート処理の正当性検証のような一点物)のみ高位モデル
- Workflow の `agent()` では `opts.model` を毎回明示的に指定する

## 4. ソース規約

- 厳格ASCII(コメント・文字列とも英語)。非ASCIIグリフはコードポイントから実行時構築
- C# 9.0 以下、ブロック名前空間のみ、Unity 2022.3 互換APIのみ
- 出荷ソース(Editor/ Runtime/)は `GlyphAuditTests` の自己監査対象

## 5. 公開リポジトリである前提

私的プロジェクト名・個人のローカルパス・内部ツール名・事故の経緯談話を、
コード・README・docs・コミットメッセージに書かない(技術的事実は中立表現で書く)。

## 6. 破壊的操作・外部公開はユーザー確認

force push・公開範囲の変更・リリースタグ・PR作成はユーザーの明示的な指示がある
時のみ行う。
